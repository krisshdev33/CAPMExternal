sap.ui.define([
    "sap/ui/test/opaQunit"
], function (opaTest) {
    "use strict";

    var Journey = {
        run: function() {
            QUnit.module("First journey");

            opaTest("Start application", function (Given, When, Then) {
                Given.iStartMyApp();

                Then.onTheA_SalesTerritoryList.iSeeThisPage();

            });


            opaTest("Navigate to ObjectPage", function (Given, When, Then) {
                // Note: this test will fail if the ListReport page doesn't show any data
                
                When.onTheA_SalesTerritoryList.onFilterBar().iExecuteSearch();
                
                Then.onTheA_SalesTerritoryList.onTable().iCheckRows();

                When.onTheA_SalesTerritoryList.onTable().iPressRow(0);
                Then.onTheA_SalesTerritoryObjectPage.iSeeThisPage();

            });

            opaTest("Teardown", function (Given, When, Then) { 
                // Cleanup
                Given.iTearDownMyApp();
            });
        }
    }

    return Journey;
});